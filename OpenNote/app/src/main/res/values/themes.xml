<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.OpenWhiteBoard" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Fullscreen Window configuration -->
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="p">shortEdges</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorPrimaryVariant">@color/brand_primary_variant</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/brand_accent</item>
        <item name="colorSecondaryVariant">@color/brand_accent</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorAccent">@color/brand_accent</item>
        <item name="android:colorAccent">@color/brand_accent</item>
        <!-- Dialog theme -->
        <item name="alertDialogTheme">@style/Theme.OpenWhiteBoard.Dialog</item>
        <item name="materialAlertDialogTheme">@style/Theme.OpenWhiteBoard.Dialog</item>
    </style>

    <style name="Theme.OpenWhiteBoard.Dialog" parent="Theme.MaterialComponents.DayNight.Dialog.Alert">
        <item name="colorPrimary">@color/brand_accent</item>
        <item name="colorSecondary">@color/brand_accent</item>
        <item name="colorAccent">@color/brand_accent</item>
        <item name="android:colorAccent">@color/brand_accent</item>
    </style>
</resources>